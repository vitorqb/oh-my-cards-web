(ns ohmycards.web.kws.cards-grid.profile.core
  "The keywords for a Card Grid Profile, a definition of a configuration for a card grid."
  (:refer-clojure :exclude [name]))

(def name "The profile name." ::name)
(def config "The configuration for the profile." ::config)
